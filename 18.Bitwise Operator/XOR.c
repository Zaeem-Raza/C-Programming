#include <stdio.h>
#include <stdlib.h>
#define SIZE 100
int main(void)
{
    /*
    * XOR is a binary operator that takes two operands and returns 1 if either of the two bits is 1 but not both.
    */
    printf("%d",1^3);   // 2 bcz 1 is 0001 and 3 is 0011
    return 0;
}